@model IEnumerable<UnitOfWorkDemo.Models.Comentario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Conteudo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Conteudo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComentarioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComentarioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComentarioId })
        </td>
    </tr>
}

</table>
